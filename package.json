{
  "name": "encoder-decoder",
  "module": "index.js",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.js",
    "build": "bun build index.js --outfile dist/index.js",
    "compile": "bun build index.js --compile --outfile dist/encoder-decoder.exe"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "cipher-guard": "^1.1.1"
  }
}
